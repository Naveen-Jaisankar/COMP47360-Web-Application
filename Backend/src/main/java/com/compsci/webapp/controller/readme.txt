This package handles all the http requests (GET, POST, PUT, DELETE) and responses. 

DailyQuizScoreController: Handles incoming HTTP requests, processes data from the user, and interacts with the service layer.