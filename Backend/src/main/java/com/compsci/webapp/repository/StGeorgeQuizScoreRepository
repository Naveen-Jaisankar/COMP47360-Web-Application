import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

@Repository
public interface StGeorgeQuizScoreRepository extends JpaRepository<StGeorgeQuizScore, Long> {
    
    StGeorgeQuizScore findByUserIdAndQuizDate(Long userId, LocalDate quizDate);
}
